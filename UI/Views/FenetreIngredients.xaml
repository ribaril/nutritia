<Window x:Class="Nutritia.UI.Views.FenetreIngredients"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Resx Titre}" Height="375" Width="400"
        Icon="/Nutritia;component/UI/Images/logoIconPetit.png" ResizeMode="NoResize"
        ResxExtension.DefaultResxName="Nutritia.UI.Ressources.Localisation.FenetreIngredient" Language="{UICulture}">
    <Window.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource fontNutritia}">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Name="grdIngredients">
        <Grid.Background>
            <ImageBrush Opacity="0.3" ImageSource="pack://application:,,,/UI/Images/background.jpg"/>
        </Grid.Background>
    </Grid>
</Window>
