<UserControl x:Class="Nutritia.UI.Views.GenerateurMenus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="475" d:DesignWidth="650">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="275"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Width="200" Height="25">Ouvrir un menu existant</Button>

        <Rectangle Grid.Row="1" Width="550"  Stroke="Black"></Rectangle>

        <Label Grid.Row="1" Margin="50, 0, 0, 0">Période :</Label>
        <StackPanel Grid.Row="1" Margin="110, 0, 0, 0">
            <RadioButton Margin="0, 5, 0, 0">Un repas</RadioButton>
            <RadioButton Margin="0, 5, 0, 0">Un jour</RadioButton>
            <RadioButton Margin="0, 5, 0, 0">Une semaine</RadioButton>
        </StackPanel>

        <Label Grid.Row="1" Margin="375, 0, 0, 0">Nombre de personne :</Label>
        <TextBox Grid.Row="1" Width="50" Height="20" VerticalAlignment="Top" Margin="425, 5, 0, 0"></TextBox>

        <Button Grid.Row="1" Margin="0, 50, 0, 0" Width="200" Height="25">Générer</Button>

        <Rectangle Grid.Row="2" Width="550" Margin="0, 10, 0, 0"  Stroke="Black"></Rectangle>

        <Button Grid.Row="3" Width="200" Height="25">Liste d'épicerie</Button>

    </Grid>

</UserControl>
